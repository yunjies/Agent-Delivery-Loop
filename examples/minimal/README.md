# Minimal Filesystem Loop

Run:

```bash
python examples/minimal/run_minimal_loop.py --reset
```

The demo creates a temporary filesystem workspace with Demand, Goal, Expert, Task, LoopDecision, Attempt, and event records.

It does not call external services or execute business work.
