[project]
name = "agent-delivery-loop"
version = "0.1.0"
description = "Framework-neutral supervised delivery loop protocol for expert agents"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
