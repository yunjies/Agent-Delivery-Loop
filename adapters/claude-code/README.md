# Claude Code Adapter

The Claude Code adapter creates serializable payloads for routing Tasks to Claude Code.

It does not execute Claude Code by itself. A controlled runner must apply policy and execute the payload.
